apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.global.prod }}-gateway
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.global.prod }}-gateway
spec:
  replicas: {{ include "replicas" . }}
  selector:
    matchLabels:
      app: {{ .Values.global.prod }}-gateway
  strategy:
    rollingUpdate:
      maxSurge: 50%
      maxUnavailable: 50%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: {{ .Values.global.prod }}-gateway
    spec:
      {{- include "common.images.pullSecrets" . | indent 6 }}
      dnsPolicy: ClusterFirst
      containers:
        - name: proxy
          image: {{ template "knoway.gateway.image" . }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          {{- if include "common.images.resources" . }}
          resources:
          {{- include "common.images.resources" . | indent 12 }}
          {{- end }}
          ports:
            - containerPort: 8080
              name: http
          volumeMounts:
            - readOnly: true
              mountPath: /app/config
              name: config
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: 10
            periodSeconds: 15
      serviceAccountName: {{ .Values.global.prod }}
      volumes:
        - name: config
          configMap:
            name: {{ .Values.global.prod }}
